<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="https://090ddf88141c4033a1a0954dcce7f49e.vfs.cloud9.ap-northeast-1.amazonaws.com/stamps/new">
        <%= image_tag 'no_image.jpg', size: '100x50' %>
      </a>
      <button class="navbar-toggler" type="button" date-toggle="collapse" data-target="#navbarNavDropdown" aria-label="Toggle navigation" aria-expanded="false">
        <span class="navbar-toggle-icon"></span>
      </button>
      <div class="collaspe navbar-collaspe" id="navbarNavDropdown"></div>
      <ul class="navbar-nav ml-auto">
        <li>
          <div class="nav-link text-light">
            <% if enployee_signed_in? %>
              <%= link_to "打刻", new_stamp_path %>
            <% end %>
          </div>
        </li>
        <li>
          <div class="nav-link text-light">
            <% if enployee_signed_in? %>
              <%= link_to "打刻時間一覧", enployee_path(current_enployee.id) %>
            <% end %>
          </div>
        </li>
        <li>
          <div class="nav-link text-light">
            <% if enployee_signed_in? %>
              <%= link_to "残業申請", new_overtime_path %>
            <% else %>
              <%= link_to "新規登録", new_enployee_registration_path %>
            <% end %>
          </div>
        </li>
        <li>
          <div class="nav-link text-light">
            <% if enployee_signed_in? %>
              <%= link_to "ログアウト", destroy_enployee_session_path, method: :delete %>
            <% else %>
              <%= link_to "ログイン", new_enployee_session_path %>
            <% end %>
          </div>
        </li>
        <li>
          <div class="nav-link text-light">
            <% if enployee_signed_in? %>
              <%= link_to "残業申請一覧", overtimes_path %>
            <% else %>

            <% end %>
          </div>
        </li>
      </ul>
    </div>
  </nav>
</header>